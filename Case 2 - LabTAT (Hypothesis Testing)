A hospital wants to determine whether there is any difference in the average Turn Around Time (TAT) of reports of the laboratories on their preferred list. 
They collected a random sample and recorded TAT for reports of 4 laboratories. TAT is defined as sample collected to report dispatch.
Analyze the data and determine whether there is any difference in average TAT among the different laboratories at 5% significance level.

# Hypothesis Testing
library(readxl)
library(readr)

LabTAT<-read.csv(file.choose())
attach(LabTAT)
View(LabTAT)

# Normality test
shapiro.test(`Laboratory.1`) #p > 0.05 => It follows normal distribution
shapiro.test(`Laboratory.2`) #p > 0.05 => It follows normal distribution
shapiro.test(`Laboratory.3`) #p > 0.05 => It follows normal distribution
shapiro.test(`Laboratory.4`) #p > 0.05 => It follows normal distribution

# Variance test
var.test(`Laboratory.1`,`Laboratory.2`) #p > 0.05 => Equal Variances
var.test(`Laboratory.1`,`Laboratory.3`) #p > 0.05 => Equal Variances
var.test(`Laboratory.1`,`Laboratory.4`) #p > 0.05 => Equal Variances

var.test(`Laboratory.2`,`Laboratory.3`) #p > 0.05 => Equal Variances
var.test(`Laboratory.2`,`Laboratory.4`) #p > 0.05 => Equal Variances

var.test(`Laboratory.3`,`Laboratory.4`) #p > 0.05 => Equal Variances

Stacked_Data <- stack(LabTAT)
View(Stacked_Data)


attach(Stacked_Data)
colnames(Stacked_Data)

Anova_results <- aov(values~ind, data = Stacked_Data)
summary(Anova_results)

# p-value = < 0.05 accept alternate hypothesis
#there is no any difference in average TAT among the different laboratories at 5% significance level.
