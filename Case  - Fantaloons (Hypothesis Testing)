Fantaloons Sales managers commented that % of males versus females walking into the store differ based on day of the week. 
Analyze the data and determine whether there is evidence at 5 % significance level to support this hypothesis.

# Hypothesis Testing
library(readxl)
library(readr)

Fantaloons<-read.csv(file.choose())
attach(Fantaloons)
View(Fantaloons)

table1 <- table(Weekdays)
table1
table2 <- table(Weekend)
table2
table3 <- table(Weekdays, Weekend)
table3

prop.test(x=c(120,47),n=c(167,66), conf.level = 0.95, alternative = "two.sided")

#p value>0.05, accept null hypothesis
